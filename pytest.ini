[pytest]
# Product tests only — the vendored components under apps/ carry their own test
# suites (run in their own repos' CI); collecting them here would re-test the
# byte-identical mirrors against this repo's layout.
testpaths = tests
norecursedirs = apps .venv data
